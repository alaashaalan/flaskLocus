def credentials():

	host="localhost"
	user="root"
	passwd="12345"
	db="locus_development"

	db_credentials = {
		'host' : host,
		'user' : user,
		'passwd' : passwd,
		'db' : db
	}

	return db_credentials